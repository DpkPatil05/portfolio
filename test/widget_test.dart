import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Landing page smoke test', (WidgetTester tester) async {
    // TODO(Deepak): Needs to be revisited
    // await tester.pumpWidget(const LaunchApp());
    // expect(find.text(Constants.strings.home), findsOneWidget);
  });
}
